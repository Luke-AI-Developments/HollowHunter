SHADOW HUNTER — Claude Code setup prompt
Paste this into Claude Code when you're ready to start (before writing any game code).
====================================================================================

I'm about to start building a Godot 4 mobile game (Android first, foreground-only
GPS, with native Health Connect + GPS plugins). Before we write ANY game code, get
my environment and Claude Code setup ready. Do all of the following and report back:

1) ENVIRONMENT AUDIT + INSTALL. Check what's installed vs missing and give me the
   exact install commands for my OS. I need:
   - Godot 4 (the .NET build if we'll use C#)
   - Git + a GitHub account/repo
   - Android Studio + Android SDK + NDK + a JDK (for Android export AND native plugins)
   - If I'm on macOS: Xcode + CocoaPods (for iOS export/plugins)
   - Godot's Android (and iOS) export templates
   - VS Code
   - GUT (Godot Unit Test) addon
   - gdtoolkit — gdformat + gdlint (pip install gdtoolkit)
   Also confirm I have a physical Android device with USB debugging (GPS + health
   do NOT work in emulators).

2) PROJECT SETUP.
   - Initialise a Git repo with a Godot .gitignore.
   - Create a structure that keeps all game logic in pure, testable scripts (separate
     from scenes/UI).
   - Generate a CLAUDE.md with our conventions: GDScript style, folder layout,
     "game logic stays pure and unit-tested," naming. Merge in the Karpathy coding
     principles (think before coding, simplicity first, surgical changes, tests-first).
   - Wire post-edit hooks that run gdformat + gdlint + GUT so you auto-format, lint,
     and test your own GDScript.

3) MCP / PLUGIN CLEANUP (unused ones waste context every session):
   - ENABLE / keep ON: Godot MCP, context7, karpathy-skills, code-review,
     code-simplifier, github, claude-md-management, feature-dev.
   - DISABLE now: caveman, typescript-lsp, playwright, gstack.
   - KEEP OFF until later (list them, don't enable): PixelLab MCP, Aseprite/pixel-plugin,
     Blender MCP (art phase only); frontend-design (UI mockups only); Figma MCP (optional).

4) After setup, DON'T start the game yet. Our first real task is a throwaway spike:
   a native Android plugin that reads GPS (FusedLocationProvider) and step/workout
   data (Health Connect) into GDScript on my physical phone. Just confirm that's next.

Give me the audit results, the exact commands to run, and a checklist of what you changed.
